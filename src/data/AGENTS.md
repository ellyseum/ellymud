# Data Directory (src) - LLM Context

## Overview

This is a placeholder directory in `src/`. Actual game data (users, rooms, items) is stored in the root `data/` directory.

## Note

If you need to work with game data, see `data/` at the project root, not this directory.

## Related Context

- [`../../data/`](../../data/) - Actual data directory with JSON files
